// configuration settings for the app

export const APP_CONFIG = {
  // Set to true to block inspection and right-click
  // Set to false to allow developer tools (for debugging)
  ENABLE_DEV_TOOLS_BLOCKER: true,
};
